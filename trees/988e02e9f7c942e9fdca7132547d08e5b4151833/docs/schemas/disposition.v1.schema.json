{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "vcs-lab.disposition/v1",
  "title": "Conflict disposition",
  "description": "One person's decision about one parked conflict, stored as an entry of <common Git dir>/vcs-lab/dispositions.json (shared-local; src/dispositions.js; ADR-0030).",
  "x-vcs-lab-scope": "shared-local",
  "type": "object",
  "required": [
    "schema", "id", "recordId", "outcome", "keptDigest", "rejectedDigests",
    "decidedAt"
  ],
  "properties": {
    "schema": { "const": "vcs-lab.disposition/v1" },
    "id": { "type": "string", "minLength": 4 },
    "recordId": { "type": "string", "minLength": 4 },
    "outcome": {
      "enum": ["keep-local", "replace-local"],
      "description": "keep-local deleted the parked copy; replace-local rewrote the local record to the parked content."
    },
    "sourceLineage": { "type": ["string", "null"] },
    "parkedRef": { "type": ["string", "null"] },
    "attachment": { "$ref": "#/$defs/oid" },
    "keptDigest": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "rejectedDigests": {
      "type": "array",
      "items": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
      "description": "Digests this clone has rejected for this identifier. A later exchange carrying one of them is reported as already disposed rather than parked again."
    },
    "reason": {
      "type": ["string", "null"],
      "description": "Free text from the person who decided; never interpreted."
    },
    "decidedAt": { "type": "string", "format": "date-time" }
  },
  "$defs": {
    "oid": { "type": "string", "pattern": "^[0-9a-fA-F]{40}$|^[0-9a-fA-F]{64}$" }
  }
}
