{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "vcs-lab.spec-manifest/v1",
  "title": "Sparse specification manifest (superseded)",
  "description": "The original tracked-portable spec manifest. Accepted on read and migrated forward on the next index; its sourceHash may have been computed over CRLF text, which readers additionally accept (src/specs.js revisionStageFromObjects). The reader requires only schema, artifactId, source, and sourceHash.",
  "x-vcs-lab-scope": "tracked",
  "deprecated": true,
  "type": "object",
  "required": ["schema", "artifactId", "source", "sourceHash"],
  "properties": {
    "schema": { "const": "vcs-lab.spec-manifest/v1" },
    "artifactId": { "type": "string", "minLength": 1 },
    "source": { "type": "string", "minLength": 1 },
    "sourceHash": { "type": "string", "pattern": "^[0-9a-fA-F]{64}$" },
    "blocks": { "type": "array" }
  }
}
