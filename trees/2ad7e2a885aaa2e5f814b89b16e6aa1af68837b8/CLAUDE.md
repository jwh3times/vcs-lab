# Claude Code instructions

The canonical repository guidance is `AGENTS.md`; this file only imports it so
Claude Code and Codex read the same rules.

@AGENTS.md

Agent skills are canonical in `.agents/skills/` and mirrored into
`.claude/skills/` by `npm run sync:agents`; edit the canonical copy only.
